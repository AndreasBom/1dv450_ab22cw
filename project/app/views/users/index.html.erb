

<h1>Alla användare</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Användare</th>
      <th>Antal Api</th>
      <th>Admin</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td>
        <%= user.first_name + user.last_name%>
      </td>
      <td>
        <%= user.apis.count %>
      </td>
      <td> <%= if is_admin?(user) then
                 'Ja'
               else 'Nej'
               end %></td>
      <td><%= link_to 'Visa användare', user %></td>
      <td><%= link_to 'Radera användare', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>

    </tr>



    <% end %>
  </tbody>
</table>

<br>

